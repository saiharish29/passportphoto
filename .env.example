# Optional — only needed if you want server-side fallback for background removal
# Get one at https://replicate.com/account/api-tokens
REPLICATE_API_TOKEN=

# Optional — public app URL for absolute links in PDF metadata
NEXT_PUBLIC_APP_URL=http://localhost:3000
